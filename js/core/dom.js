// DOM utility functions (currently empty due to original code not having standalone DOM helpers)
// Placeholder to avoid empty file requirement
export function getElementById(id) {
  return document.getElementById(id);
}
